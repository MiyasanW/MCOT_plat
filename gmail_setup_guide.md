# การตั้งค่า Gmail SMTP สำหรับ MCOT Rental Platform

ระบบใช้ Gmail SMTP สำหรับส่งอีเมล (ใบเสนอราคา, แจ้งเตือนการจอง ฯลฯ) ต้องใช้ **App Password** ไม่ใช้รหัสผ่านล็อกอินปกติ

---

## 1. สร้าง App Password (ทำครั้งเดียว)

1. เข้า [Google Account → Security](https://myaccount.google.com/security) แล้วล็อกอินด้วยบัญชี Gmail / Google Workspace ที่จะใช้ส่งเมล

2. **เปิด 2-Step Verification** (ถ้ายังไม่เปิด)  
   - หัวข้อ **How you sign in to Google** → **2-Step Verification** → เปิดใช้งาน

3. **สร้าง App Password**  
   - ในหน้า Security ค้นหา **"App passwords"** (รหัสผ่านสำหรับแอป)  
   - ด้านล่างเลือกแอป: **Mail** และอุปกรณ์: **Other** แล้วตั้งชื่อ เช่น `MCOT Rental`  
   - กด **Create** จะได้รหัส 16 ตัว (รูปแบบ `xxxx xxxx xxxx xxxx`)  
   - **Copy รหัสนี้** (ลบช่องว่างก็ได้) ไว้ใส่ใน `.env`

---

## 2. ตั้งค่าในโปรเจกต์

ในโฟลเดอร์โปรเจกต์สร้างหรือแก้ไขไฟล์ **`.env`** (อย่า commit ไฟล์นี้ขึ้น Git):

```env
# อีเมลที่ใช้ส่ง (ต้องเป็นบัญชีที่สร้าง App Password)
EMAIL_HOST_USER=your-email@gmail.com
EMAIL_HOST_PASSWORD=xxxxyyyyzzzzwwww

# ถ้าใช้ Gmail / Google Workspace แบบมาตรฐาน
EMAIL_HOST=smtp.gmail.com
EMAIL_PORT=587
EMAIL_USE_TLS=True
DEFAULT_FROM_EMAIL=MCOT Rental <your-email@gmail.com>
```

- แทน `your-email@gmail.com` ด้วยอีเมลจริง  
- แทน `xxxxyyyyzzzzwwww` ด้วย App Password 16 ตัว (ไม่มีช่องว่างก็ได้)

---

## 3. ตรวจสอบใน Django

โปรเจกต์อ่านค่าอีเมลจาก `.env` ใน `config/settings.py` แล้ว  
ถ้าไม่ใส่ `EMAIL_HOST_USER` / `EMAIL_HOST_PASSWORD` ระบบจะใช้ **Console Backend** (เมลแสดงในเทอร์มินัล ไม่ส่งจริง)

---

## 4. ทดสอบส่งเมลให้ลูกค้า

จากโฟลเดอร์โปรเจกต์รัน:

```bash
python manage.py send_test_email ลูกค้า@email.com
```

ถ้าไม่มี error และลูกค้าได้รับเมล แสดงว่าตั้งค่าส่งให้ลูกค้าพร้อมใช้งาน  
(ถ้ายังใช้ Console Backend จะมีข้อความแจ้งและเมลจะโผล่ในเทอร์มินัล)

---

## 5. รีเซ็ตรหัสผ่าน — ทำไมใช้ไม่ได้ / ไม่มีเมลมา

การกด "รีเซ็ทรหัสผ่าน" ระบบจะส่งเมลจาก **ค่า EMAIL ใน .env** และ **ลิงก์ในเมล** มาจากการตั้งค่า **Site domain** ดังนั้นต้องเช็ค 3 อย่าง:

1. **ตรวจว่าเมลส่งจริงหรือไม่**  
   - ถ้าใน `.env` ยังเป็น placeholder (`your-email@gmail.com` / `your-16-char-app-password`) ระบบจะใช้ **Console Backend** → เมลไม่ส่งจริง แค่โผล่ในเทอร์มินัลที่รัน `runserver`  
   - **แก้:** ใส่ Gmail จริง + App Password ตาม [ข้อ 1–2](#1-สร้าง-app-password-ทำครั้งเดียว) แล้วรัน `python manage.py send_test_email อีเมลคุณ@gmail.com` ให้ได้เมลจริงก่อน

2. **ตั้งค่า Domain ของลิงก์ในเมล (สำคัญ)**  
   - ลิงก์ "ตั้งรหัสผ่านใหม่" ในอีเมลสร้างจาก **Django Sites** (SITE_ID=1) ถ้า domain ผิด (เช่น example.com) คลิกแล้วเข้าไม่ได้  
   - **แก้:** รันคำสั่งด้านล่างให้ตรงกับที่คุณเข้าเว็บ (รัน local = 127.0.0.1:8000, production = โดเมนจริง เช่น mcot-rental.com)  
     ```bash
     python manage.py set_site_domain 127.0.0.1:8000
     ```  
   - หรือใส่ใน `.env`: `SITE_DOMAIN=127.0.0.1:8000` แล้วรัน `python manage.py set_site_domain`  
   - ทางเลือก: ไป **Admin → Sites** แก้ domain ของ Site id=1 ให้ตรงกับที่รันเว็บ

3. **อีเมลที่กรอกต้องเป็นอีเมลที่สมัครในระบบ**  
   - ระบบส่งเมลรีเซ็ตเฉพาะอีเมลที่มีบัญชี User ในระบบเท่านั้น  
   - **ตรวจว่าอีเมลมีในระบบหรือไม่:**  
     ```bash
     python manage.py check_user_email ลูกค้า@email.com
     ```  
     - ถ้า "ไม่พบบัญชี" แปลว่าลูกค้ายังไม่ได้สมัครด้วยอีเมลนี้ หรือสมัครด้วยอีเมลอื่น

4. **ตรวจกล่องสแปม / Promotions**  
   - บางครั้งเมลไปอยู่โฟลเดอร์สแปมหรือ Promotions ของ Gmail

**สรุป:** ใส่ SMTP จริง → รัน `set_site_domain` ให้ตรงกับที่เข้าเว็บ → ทดสอบ `send_test_email` → ใช้รีเซ็ตด้วยอีเมลที่ `check_user_email` พบในระบบ → ตรวจสแปมถ้าไม่เห็นเมล

---

## 5.1 ทำไมเมลทดสอบไม่โดนสแปม แต่เมลจริง (รีเซ็ต/ใบเสนอราคา) โดน

เมลทดสอบกับเมลใช้จริงต่างกันแบบนี้ เลย Gmail จัดแยกไม่เหมือนกัน:

| | เมลทดสอบ | เมลจริง (รีเซ็ตรหัสผ่าน / ใบเสนอราคา) |
|---|----------|----------------------------------------|
| **เนื้อหา** | ข้อความสั้น ไม่มีลิงก์ ไม่มีไฟล์แนบ | มี **ลิงก์** (รีเซ็ต) หรือ **ไฟล์ PDF แนบ** (ใบเสนอราคา) |
| **ผลต่อสแปม** | ดู “ปลอดภัย” → มักเข้า Inbox | ลิงก์ไปที่ 127.0.0.1 / IP หรือมีไฟล์แนบ → Gmail มักจัดเป็นสแปมหรือ Promotions |

**สาเหตุที่เมลจริงโดนสแปม:**

- **รีเซ็ตรหัสผ่าน:** ในเมลมีลิงก์ไปตั้งรหัสผ่านใหม่ ถ้าโดเมนเป็น **127.0.0.1** หรือ **IP/โดเมนแปลกๆ** Gmail จะไม่ไว้ใจ (เหมือนรูปแบบ phishing) → ย้ายไปสแปม/Promotions
- **ใบเสนอราคา:** มี **ไฟล์ PDF แนบ** จากผู้ส่งที่ยังไม่เคยมีประวัติ (domain reputation) → โอกาสโดนสแปมสูง

**สิ่งที่ทำได้:**

1. **ตอนทดสอบ / ใช้จริงบนเครื่องตัวเอง**  
   - เปิดกล่องสแปม (และ Promotions) แล้วกด **“ไม่ใช่สแปม” / “Mark as Not spam”**  
   - หรือเพิ่มอีเมลผู้ส่ง (ที่ใส่ใน `DEFAULT_FROM_EMAIL`) เป็น **ผู้ติดต่อ** ใน Gmail เพื่อให้เมลถัดไปมีโอกาสเข้า Inbox มากขึ้น

2. **ตอนขึ้น production จริง**  
   - ใช้ **โดเมนของตัวเอง** (เช่น mcot-rental.com) เป็นลิงก์ในเมล และตั้ง **SITE_DOMAIN** ให้เป็นโดเมนนั้น  
   - ตั้ง **SPF / DKIM** ให้โดเมนที่ใช้ส่งเมล (หรือใช้ Gmail/Google Workspace กับโดเมนนั้น) เพื่อให้ Gmail เชื่อถือผู้ส่งมากขึ้น เมลจริงจะโดนสแปมน้อยลง

3. **ถ้าต้องการให้เมลทดสอบ “เหมือน” เมลจริงมากขึ้น**  
   - เมลทดสอบตอนนี้เป็นแค่ข้อความธรรมดา ไม่มีลิงก์/แนบ จึงไม่โดนสแปม  
   - เมลใช้จริงจะยังมีลิงก์/แนบอยู่ดี ดังนั้นการ “ทำให้เหมือนกัน” จะไม่ช่วยเรื่องสแปมมาก แนะนำให้ทำข้อ 1 และ 2 แทน

---

## 6. ตรวจเมลกับผู้ใช้ (ยืนยันอีเมล)

ตอนนี้ระบบตั้งเป็น **ไม่บังคับยืนยันอีเมล** (`ACCOUNT_EMAIL_VERIFICATION = 'none'`) คือสมัครแล้วใช้ได้เลย

ถ้าต้องการให้ **ผู้ใช้ต้องยืนยันอีเมลก่อนใช้งาน** (คลิกลิงก์ในเมลก่อนถึงจะล็อกอินได้):

- ใน `config/settings.py` เปลี่ยนเป็น  
  `ACCOUNT_EMAIL_VERIFICATION = 'mandatory'`  
- การส่งเมลยืนยันอีเมลใช้ SMTP ตัวเดียวกับรีเซ็ตรหัสผ่าน (ค่าใน `.env`)  
- ต้องตั้งค่า SMTP ให้ส่งได้จริงก่อน ถึงจะยืนยันอีเมลได้

ถ้าไม่ต้องการบังคับยืนยัน แค่ใช้เมลเป็นตัวล็อกอินและส่งรีเซ็ตรหัสผ่าน ตั้งค่าเหมือนตอนนี้ได้

---

## 7. ความปลอดภัย (รหัสผ่าน + รีเซ็ต)

- **รหัสผ่าน:** ระบบใช้ Django password hashing (PBKDF2) และตรวจรหัสผ่านด้วย `AUTH_PASSWORD_VALIDATORS` (ความยาวอย่างน้อย 8 ตัว, ไม่ใช้รหัสที่นิยม, ไม่เป็นตัวเลขอย่างเดียว ฯลฯ)
- **รีเซ็ตรหัสผ่าน:** ใช้ **django-allauth** + Django token
  - **วันหมดอายุ:** ลิงก์ในอีเมลใช้ได้ **30 นาที** (กำหนดโดย `PASSWORD_RESET_TIMEOUT` ใน settings)
  - **ใช้ได้ครั้งเดียว:** หลังกดลิงก์แล้วตั้งรหัสผ่านใหม่ token จะหมดอายุทันที (ใช้ซ้ำไม่ได้)
  - **ป้องกันการปลอม:** token ลงนามด้วย SECRET_KEY (salted HMAC) แก้ไขหรือเดาไม่ได้
  - **Rate limit:** จำกัดจำนวนครั้งขอรีเซ็ตต่อ IP (ของ allauth)
- ไม่ต้องติดตั้ง library เพิ่มสำหรับ flow รีเซ็ต; ถ้าต้องการจำกัดการลองล็อกอินเพิ่มเติม สามารถใช้ `django-axes` ได้
